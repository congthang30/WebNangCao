@page
@model NewWeb.Pages.Account.SetPasswordModel
@{
    ViewData["Title"] = "Đặt mật khẩu";
}

<link rel="stylesheet" href="~/css/account.css" asp-append-version="true" />

<div class="account-container">
    <div class="account-card">
        <div class="account-header">
            <h2><i class="bi bi-shield-check me-2"></i>Đặt mật khẩu</h2>
            <p class="text-muted">Hoàn tất đăng ký bằng Google</p>
        </div>

        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle me-2"></i>@Model.ErrorMessage
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        }

        <div class="alert alert-info mb-4">
            <i class="bi bi-info-circle me-2"></i>
            <strong>Thông tin tài khoản:</strong>
            <p class="mb-1 mt-2"><strong>Email:</strong> @Model.Email</p>
            <p class="mb-0"><strong>Họ tên:</strong> @Model.UserName</p>
        </div>

        <div class="alert alert-warning mb-4">
            <i class="bi bi-exclamation-triangle me-2"></i>
            Vui lòng đặt mật khẩu để hoàn tất đăng ký. Mật khẩu này sẽ được sử dụng khi bạn đăng nhập lần sau.
        </div>

        <form method="post" class="account-form">
            <div class="mb-3">
                <label asp-for="Password" class="form-label">
                    <i class="bi bi-lock me-2"></i>Mật khẩu *
                </label>
                <input asp-for="Password" type="password" class="form-control" 
                       placeholder="Nhập mật khẩu (tối thiểu 8 ký tự)" required />
                <small class="text-muted">Mật khẩu phải có ít nhất 8 ký tự, bao gồm chữ hoa và chữ thường</small>
            </div>

            <div class="mb-3">
                <label asp-for="ConfirmPassword" class="form-label">
                    <i class="bi bi-lock-fill me-2"></i>Xác nhận mật khẩu *
                </label>
                <input asp-for="ConfirmPassword" type="password" class="form-control" 
                       placeholder="Nhập lại mật khẩu" required />
            </div>

            <div class="d-grid gap-2 mb-3">
                <button type="submit" class="btn btn-primary btn-lg">
                    <i class="bi bi-check-circle me-2"></i>Hoàn tất đăng ký
                </button>
            </div>

            <div class="text-center">
                <p class="text-muted mb-0">
                    <small>Bằng cách đăng ký, bạn đồng ý với điều khoản sử dụng của chúng tôi</small>
                </p>
            </div>
        </form>
    </div>
</div>

